// ============================================================
//  ⚠️  API 키 템플릿 파일 — 로컬에서는 사용되지 않으며 예시용입니다.
//  실제 실행 시 server.js 가 .env 파일의 내용을 읽어
//  가상의 config.js를 프론트엔드로 전달합니다.
// ============================================================

const CONFIG = {
  // 배포 시 .env 파일에 등록해야 할 환경변수 목록:
  // OPINET_API_KEY="오피넷 API 키"
  // GOOGLE_MAPS_API_KEY="구글 지도 API 키"

  DEFAULT_RADIUS: 3000,
  DEFAULT_FUEL: "B027",
  USE_MOCK_DATA: false,
};
